// Walking skeleton: the only job of this SPA is to prove the deploy pipeline
// (SPA + API + Cron from one Worker). Real UI comes in Phase 1.
export function App() {
  return (
    <main>
      <h1>苔むす</h1>
      <p>歩く骨格。まだ何も生えていない。</p>
    </main>
  );
}
