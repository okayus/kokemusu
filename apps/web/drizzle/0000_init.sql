-- Phase 0 skeleton: empty migration that validates the D1 migration pipeline
-- end-to-end (migrations_dir wiring, the build token's D1 Edit permission, a real
-- database_id). Real schema arrives in Phase 1 (docs/data-model.md) — read the
-- cloudflare-d1-drizzle-migration skill before the first real migration.
SELECT 1;
