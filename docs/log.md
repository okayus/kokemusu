# ログ（追記専用・新しい順）

1 行 = 1 節目（PR の merge・ADR・人手作業の完了・本番の状態変化）。`- YYYY-MM-DD 何を（#PR / ADR / skill）`。
自動ロードはされない。必要なら `head -20 docs/log.md`。作業中の試行錯誤は書かない（git log と PR にある）。

- 2026-09-12 **厚み PR 2（集計）完了 = merge #58 + 本番デプロイ確認（Workers Builds success、バンドルに `amount`・「量 」到着）— migration なし、auto-merge**（石のつながりのノード・橋と年表の行が量を読む。core `stacking.ts` に `totalAmount(stackings, window?)` / `amountByMonth(stackings)`（各月 その月に重なる日数 × 厚み）、`day.ts` に `lastDayOfMonth`。`/api/stats/graph` は GROUP BY を外して 苔片 × 石 `(id, name, norm, color, first_day, last_day, thickness)` と 苔片 × 組 `(a, b, first_day, last_day, thickness)` の行を返し、`buildGraph(nodeRows, edgeRows, window?)` が `decodeStacking` → `amountOf` で足す（窓 ＝ 今月 / 今年 は 初日〜今日、全期間は無し）。wire は `count` → `amount`。`/api/stats/timeline` は軸の行に `thickness` を足し `rowAmounts` / `rowAmountsByTag` が行の `amount` と `months: { month, amount }` を出す（default / focus / tags の 3 形。行の `count` は残す）。SPA は `src/amount.ts`（`formatAmount`: 1 以上は整数・1 未満は 1 桁、`measuredThickness` / `formatThickness`）、石・橋の名前「量 N」、`nodeRadius` / `edgeWidth` は `amount`（目盛り据え置き、量 ≈ 46 で飽和）、年表の注記「量 N · 厚み x%」、セグメント title「YYYY-MM · 量 N」。棒の title「N 片 · 活動 x/y か月」と「0 片」は枚数のまま、総草は不変。枚数の `bucketSpansByMonth` は呼び手が消えたので削除。docs は visualization §6 / §8・data-model・features・roadmap・plans/thickness.md（PR 2 ✅ ＋ plan との差分）。検証: ルートで `pnpm run ci`（単体 462）、e2e 11/11（golden path に 10 日 × 厚み 60 の続く苔片 → `amount` 6 / `count` 1 / months の和 6 / `?period=month` は今月に重なる日数 × 0.6、リロード後に石「案件 · 量 6」と注記「量 6 · 厚み 60%」）。**plan と違えた点 2 つ**: ① `months` の和は行の量に**等しい**（月は日を分割する。ADR-0007 の「量以上」は等号で成立）② `amountByMonth` は `Stacking` を受けるので `stacking.ts` 側。**罠 2 つ**: ① graph の SQL は GROUP BY 無しで苔片 × 組の行 → 99 タグの苔片 1 つで 4851 行（個人日記の規模では放置、増えたら共起の下限で間引く — visualization §6）② e2e で in-page fetch で積んだ苔片は `refreshKey` が動かないので、石・年表の断定は `page.reload()` の後）
- 2026-09-11 **苔片のタグ上限 20 → 99 完了 = merge #57 + 本番デプロイ確認（Workers Builds success、バンドルに `>=99` 到着）— migration なし、auto-merge**（送り側 mazuoboeru が 1 苔片に最大 99 タグを送るための受け側対応。`MAX_TAGS_PER_POST = D1_MAX_BOUND_PARAMETERS(100) − 1`: 書く側の最悪の 1 文 `resolveTagRows` の `user_id = ? AND norm IN (…)` が 1 + n、後続の batch は 1 文あたり数個で、D1 の上限は「文ごと」（limits ページ）。**送り側メモの「`?tags=` もこの定数を追う」は不成立**: drizzle の `toSQL().params.length` の実測で、投稿一覧の `?tags=` はカーソル・期間・limit 込みで n + 11（n ≤ 89）、年表 `?focus=` の共起 2 文は IN と NOT IN で 2n + 2（n ≤ 49）→ 集合は `MAX_TAGS_PER_SET = 20` に分離して据え置き（選んだ石は 1 石ずつ・橋で 2 石、query 文字列の 1400 もそのまま）。`tag.test.ts` に算術を pin、`posts.test.ts` の 21 を定数に、`src/tags.ts` の `MAX_TAGS` も 99。契約の説明文を定数から生成して `docs/senders/posts.schema.json` を `vitest -u` で再生成（`maxItems` 99）、`docs/senders.md` 変更履歴に 1 行。公開送り側の `kokemusu-post.ts` は `tags` 固定 1 個で影響なし。検証: `pnpm run ci`（単体 444）、e2e 11/11、使い捨て spec でローカル D1 に 99 タグ POST 201（post_tags 99 / tag 99）・100 タグ 400・PATCH で 99 個付け替え（200 文の batch）200・集合 20 は 200 / 21 は 400。**罠 2 つ**: ① apps/web 配下の `pnpm ci` は pnpm 本体の未実装コマンドに化ける → ルートで `pnpm run ci` ② タグ数に比例する SQL の bound parameter は目で数えず、`createDb({} as D1Database)` で組んだ query の `toSQL().params.length` を使い捨て vitest で測る（D1 不要・数十秒））
- 2026-09-09 **送り側が読む契約の公開 完了 = merge #56（ADR-0008、人間 merge）— migration なし**（`docs/senders.md` ＝ 読み方（raw GitHub から vendoring）・PAT の前提・`POST /api/posts` の規則と日の規則・エラー表・送り側の作法・公開送り側一覧・変更履歴 ＋ `createPostSchema` から `senders-contract.test.ts` が生成し `toMatchFileSnapshot` で一致検査する `docs/senders/posts.schema.json`（`FIELD_DOCS` は `Record<Field, string>` なので新キーの説明漏れは `tsc` で落ちる）。送り側は `z.fromJSONSchema` の契約テストを持つ。本番デプロイ確認は #57 と合わせて）
- 2026-09-09 **厚み PR 1（DB ＋ core ＋ API）完了 = merge #55 + 本番デプロイ確認（Workers Builds success ＝ `0006` が本番 D1 に適用、バンドルに `thickness` 到着。#54 の ADR-0007 も同日 merge）— migration あり、持ち主の指示で `--auto --squash` を arm**（`post` の 2 度目の再構築 `0006_post_thickness.sql`: `thickness` ＋ CHECK 3 つ（`post_days_ordered` / `post_thickness_iff_span` / `post_thickness_range`）を 0004 の recipe で手書き（`post_tags_keep` 退避 → `__new_post` → backfill ＝ 続く苔片 100 / 単日 NULL → DROP / RENAME / index → `INSERT OR IGNORE` で復元、PRAGMA 無し）。guard の免除 2 本目で文の並びと PRAGMA 無しを固定。リハーサルは `.wrangler/e2e` の写し: post 8 / post_tags 7 / tag 5 不変、`sqlite_master` に CHECK 3 つ、単日に 60 / 範囲に NULL / 0 / 101 / 逆転 の UPDATE がそれぞれの CHECK 名で落ち、再適用は no-op。本番の続く苔片は merge 直前で 0 件（backfill の対象なし、merge 前の export は取らず）。core `worker/core/stacking.ts`（純粋関数のみ）: branded `Thickness`（zod が唯一の constructor）、`Stacking = day | days`、`parseStacking`（wire → 型。単日 ＋ 数・範囲 − 厚み・範囲 ＋ null は null ＝ 400）、`patchStacking`（PATCH の三値: undefined ＝ 据え置き / null ＝ 無し / 数 ＝ 付け替え。`??` で潰さず `=== undefined`）、`decodeStacking`（CHECK が守るはずの形が崩れた行は throw ＝ fail closed）、`encodeStacking`（NULL が生まれる唯一の場所）、`daySpanOf` / `spanDays` / `amountOf`（窓でクリップ、731 × 60% ＝ 438.6）/ `measuredThickness`。`day.ts` の `canStackOn` は畳んで削除、`countDays` を追加。API: `createPostSchema.thickness`（1〜100 の整数 | null | 省略）、POST は `parseStacking`、PATCH は `patchStacking(decodeStacking(owned))`、応答（create / list / PATCH）に `thickness: number | null`。SPA は `PostItem.thickness` / `PostInput.thickness`。単体 439（+50）、e2e 11/11（PAT spec に 単日 ＋ 厚み 400 / 範囲だけ 400 / 範囲 ＋ null 400 / 範囲 ＋ 60 は 201 で応答と D1 に 60、golden path に 画面の続く苔片 ＝ 100 と PATCH の 縮めて厚み残り / 厚みだけ外す 400）。ローカル dev D1 は 0004〜0006 が未適用だったので `pnpm db:migrate` で 3 本まとめて適用。**罠 3 つ**: ① drizzle-kit 0.31.10 は `check()` の追加を `PRAGMA foreign_keys=OFF` 付き・退避なしの再構築で吐き、CHECK の列参照を `"__new_post"."col"` で書く（RENAME 後の解決に賭けない）→ snapshot（`checkConstraints` は `"post"."col"`）と journal だけ残し（ファイル名と `tag` を意味のある名前に）、SQL は裸の列名で手書き。再 generate は no-op ② 厳しい wire（範囲に厚み必須）は PR 1 の merge 直後から画面で範囲を積むと 400 になる → `stackDaysInput(first, last, current)` が範囲に `current ?? 100`・単日に null を送るつなぎを PR 1 に入れた（積むは 100 固定、編集は `p.thickness`。PR 3 でスライダー値に置き換え） ③ `wrangler d1 execute --json` の出力は先頭に wrangler のバナーが混ざる → `[` からを JSON.parse する node ワンライナーで `results` か `CHECK constraint failed: <name>` の行を出す関数にすると、リハーサルの CHECK 検査が 1 コマンド 1 断定で書ける）
- 2026-09-09 **厚み 決定 = ADR-0007（`grill-with-docs` 2 回。PR #54 = docs のみ、人の merge 待ち）**（続く苔片は「厚み」（期間のうち打ち込んでいた日の割合、1〜100%）を必ず持ち、量 ＝ 日数 × 厚み。単日は 1 で厚みを持たない（1 枚 ＝ 1 日 × 100%、基準は「100% ＝ 毎日 1 枚」）。1 回目の grill は「厚みなし ＝ 量 1」の sheet 変種を NULL で残す形だったが、持ち主が「既存データに破壊的でいい、コードが意味を表現する設計に」と再 grill → sheet を消し、「単日 | 続く(厚み)」の直和を core の型（`Stacking`、branded `Thickness`、`parseStacking` / `decodeStacking` / `encodeStacking`）・DB の CHECK 3 つ（`post` の 2 度目の再構築、既存の続く苔片は 100% で backfill して画面で直す）・wire の解釈（範囲に厚み必須、単日に厚み 400、PATCH は据え置き / null ＝ 無し）・テストの法則 で同じ形に。量を読むのは 石・橋・年表の注記「量 N · 厚み x%」と活動月・累積、総草と「計 N 片」は枚数のまま、期間はクリップ。UI は範囲になったときだけスライダー（初期 100、目盛り 14 / 60 / 71 / 100）＋ 換算。用語集に 厚み・量、続く苔片を改訂。実装は plans/thickness.md の PR 1 再構築 ＋ core ＋ API（人の merge）→ PR 2 集計 → PR 3 UI、エクスポートより先）
- 2026-09-07 **見かた 2 つ（投稿一覧 / 年表）と「選んだ石」完了 = merge #53（plans/tabs-and-stones.md PR 2。plans は完了で削除）+ 本番デプロイ確認（Workers Builds success。実データ目視は残）— migration なし、auto-merge**（sticky バーに `<nav aria-label="見かた">`（`/` ＝ 投稿一覧、`/年表`）、`src/view.ts` の `useView` が URL ↔ 見かた。選んだ石（`src/stones.ts`、純粋関数）は石で足し引き・橋は両端まとめて、1 つの状態を投稿一覧の AND 絞り込みと年表の軸が共有。年表から積むと見かたを動かさず「積みました」＋「投稿一覧へ」。総草は投稿一覧だけ。docs: features §3、visualization §6 §8、CONTEXT.md（見かた・投稿一覧・年表・選んだ石）、roadmap）
- 2026-09-07 **年表の focus を石の集合に 完了 = merge #52（plans/tabs-and-stones.md PR 1）— migration なし、auto-merge**（`GET /api/stats/timeline?focus=a,b…`（1〜20、`parseFocusParam`）で「集合が全部付いた苔片」の 1 行 ＋ 集合 × 共起する石の行（年表順）。SQL は `?tags=` と同じ `HAVING COUNT(DISTINCT tag_id) = n` ＋ `tag_id NOT IN S`、5 文 1 batch。1 石は #30 以来と同じ結果。組み立ては `buildFocusRows` の純粋関数）
- 2026-09-06 **積むダイアログの日の欄をタグの下へ・畳みを外す 完了 = merge #51 — migration なし、auto-merge**（日は苔片を書くことの一部で、畳むと過去に積めることに気づけない。`fieldset` の `legend` を見える「積む日（任意）」に。編集フォームは `<details>`「日を選ぶ」のまま。単体 389、e2e 11/11）

- 2026-09-06 **タグ欄を Notion 風のチップ ＋ 候補に 完了 = merge #50 + 本番デプロイ確認（Workers Builds success、バンドルに「タグ（任意）」/「…を新しい石に」/ `combobox`、CSS に `.tag-field-box` / `-options` / `-chip` 到着。実データ目視も OK）— migration なし、auto-merge**（`TagField`（積むダイアログと編集フォームで共用）＝ WAI-ARIA の editable combobox（`role=combobox` ＋ `aria-expanded` / `aria-controls` / `aria-activedescendant` → `role=listbox`）。候補は前方一致 → 部分一致、照合は正規化キー（`src/tags.ts` の `normalizeTagName` ＝ `worker/core/tag.ts` の写し）なので幅・大小で隠れない。綴りが無ければ末尾に「「…」を新しい石に」、空欄フォーカスで残りの石を全部（眺める用）。キー: Enter / Tab / カンマ（半角・全角・読点）で確定、**空欄の Enter は何もしない**（投稿は ⌘/Ctrl+Enter のまま）、空欄の Backspace で末尾のチップを外す、↑↓ で候補を歩く、Escape は候補だけ畳んでダイアログは残す。IME 変換中のキーは触らない（`isComposing` ＋ `compositionend` で吸収）。書きかけの文字は投稿・保存の時点で石になる（`stonesOf`）。純粋関数は `src/tags.ts` に分離（`suggestStones` / `absorbSeparators` / `commitText` / `addStones`、上限 20 ＝ `MAX_TAGS_PER_POST`）、`submitOnCmdEnter` は `src/form.ts` へ。下書きは `tags` が配列 ＋ `tagText`（旧の文字列形は同じ規則で分割して読む）、同じ石に積む の seed も名前の配列。`<datalist>` と `splitTagField` は撤去し `tagOptions` を ComposeDialog / Timeline / PostEntry へ。単体 387（+17）、e2e 11/11（ヘルパ `e2e/helpers/tags.ts` の `fillTags` / `tagChips`、ゴールデンパスに 候補 → Enter・未登録の綴り・Escape は候補だけ・空欄の Backspace・編集フォームは石のチップから 1 つ足す）。使い捨て spec でライト / ダーク / 390px / 未登録 / 空欄で眺める / 編集フォームを目視。**罠 2 つ**: ① 候補を欄の**直下に in-flow** で出すと「積む」を押した瞬間に blur → リストが畳まれる → ボタンが上へ動く → mouseup が別要素に落ちて**クリックが発火しない**（ゴールデンパスがここで落ちた）→ 欄の**上に重なる overlay**（`position: absolute` / `inset-block-end: 100%`、`z-index` は sticky バー 1・`.fab` 2 の上）に。anchor positioning は使わない（fallback を持たずに済む）② React は `autoComplete` を属性名そのままで吐く — SSR の markup テストを `autocomplete="off"` で書くと当たらない）
- 2026-09-06 **見出し（`post.title`）廃止 完了 = ADR-0006 + merge #49（人手）+ 本番デプロイ確認（Workers Builds success ＝ `0005` が本番 D1 に適用。バンドルから「見出しを付ける」「見出し（任意）」、CSS から `.post-title` が消えたことを実物で確認。実データ目視も OK）**（決めること 7［2026-08-23］を撤回 —— 苔片は Twitter のように思ったこと・メモに石を付けるだけで、題が要る文章は本文の Markdown 見出しで書く。ダイアログ化（#41）で載せた `<details>`「見出しを付ける」を人が使う場面が無かった。既存の見出しは消えてよい［持ち主の判断］。`0005_drop_post_title.sql` ＝ `ALTER TABLE post DROP COLUMN title` の 1 文（0003 と同じ引き算で再構築ではない。index 無し ＝ DROP INDEX も無し、PRAGMA 無し、`post_tags` に触れない）。API は `createPostSchema` を **`z.strictObject`** に ＝ `title` を含む知らないキーは 400（黙って捨てると送り側が気づけない。ADR-0002 の「受け側は検証するだけ」の帰結）。create / list / PATCH から title の暗号化・復号・select を除去し、暗号文の列は `body` だけに。UI はダイアログの「見出しを付ける」・編集フォームの欄・カードの `.post-title`・削除確認の見出しを削除。端末に残った下書きの `title` は読むときに本文の 1 行目へ写す（書きかけを黙って捨てない）。単体 370、e2e 11/11（ゴールデンパスの見出しの手を外し、PAT spec に「`title` 付き body は 400」）。docs: features §1/§2/§3/§7・data-model・roadmap（決めること 7 に撤回）・CONTEXT.md（見出しの項を削除）・security・visualization・CLAUDE.md、ADR-0001 / 0002 に補記。**罠 1 つ**: `migrations.test.ts` の rebuild ガードは**コメント行も含めてファイル全文**を正規表現で見るので、migration のヘッダーに「再構築ではない（no DROP TABLE）」と書くと `DROP TABLE` にマッチして落ちる → 文言を変える。リハーサルは `.wrangler/e2e` の写しに `--persist-to` で適用し post 11 / post_tags 13 / tag 8 が不変・`pragma_table_info('post')` から `title` 消滅を確認）
- 2026-09-06 **右下の丸い「積む」完了 = merge #48 + 本番デプロイ確認（Workers Builds success、CSS に `.fab` / `safe-area-inset` 到着。実データ目視も OK）— migration なし、auto-merge**（`position: fixed` の 56px の丸（苔色に「＋」の SVG、アイコンだけなので名前は `aria-label="積む"`）を右下に。`env(safe-area-inset-*)` を足し `.shell` の下余白を 5rem → 6.5rem に ＝ 最後の苔片の操作に被らない。`.shell:has(dialog[open]) .fab { visibility: hidden }` でモーダル中は消す（背景越しの押せないボタンを出さない、a11y ツリーからも消える。閉じるとフォーカスは丸いボタンへ戻る）。ヘッダーの「積む」は削除し、sticky は受領ピル（`.bar-notice`）のために残す。`n` キーと 同じ石に積む は不変。単体 366、e2e 11/11（ゴールデンパスの `stack` を `.fab` ＋ アクセシブルネーム「積む」で引き直し）。使い捨て spec でライト / ダーク / 390px / 最下部 / フォーカスの輪 / ダイアログ中は隠れる を目視）
- 2026-09-06 **A2 過去に積む・続く苔片 完了 = merge #47 + 本番デプロイ確認（Workers Builds success、バンドルに「日を選ぶ」「〜いつまで」「に積む」「に絞る」、CSS に `.days-field` / `.compose-days` / `.post-posted` 到着。実データ目視は残）— migration なし、auto-merge。plans/day-axis-and-kind.md は完了で削除**（`POST` / `PATCH /api/posts` の body に `firstDay` / `lastDay`（POST は省略 ＝ 今日・`lastDay` 省略 ＝ `firstDay`、**PATCH は省略した側を据え置き** — 日を言わない編集は日を動かさない、伸ばすのは `lastDay` を書く PATCH。PAT も同じ）。検証は core の `canStackOn`（暦にある日・`first ≤ last ≤ today`・`earliestStackDay` ＝ 今日の月を含め 1200 か月前の月初 ＝ `enumerateMonths` の上限と同じ床）で外れれば 400、`postedDay` は編集でも不変。UI: 積むダイアログと編集フォームに `<details>`「日を選ぶ」（date 2 欄「いつ」「〜いつまで」、`min` ＝ 床・`max` ＝ 一覧の `today`・互いに縛る → 逆転・未来は `rangeOverflow`。畳むと summary に日。`draft.ts` に日）、カードは いま積んだ苔片だけ時刻・それ以外は日（範囲）＋「M/D に積む」（年が違えば Y/M/D）、着地は過去 / 続く苔片なら一覧を動かさず `role=status`「積みました」＋ ボタン「YYYY/MM/DD に絞る」（石を持たなければ石も外す）、読み込み済みページ内なら `placeInFeed` で並びの位置へ（編集で日が動いた苔片も同じ）。単体 366（+30）、e2e 11/11（at rest の `first_day` 細工を編集フォームの PATCH に置き換え、末尾に A2 の 1 手: 昨日に 1 枚 → 受領ボタンで絞る・時刻無し・昨日のマス +1 ／ 2 日の続く苔片 → 両日 +1 だが計は +1・年表の行と月・今日の窓で出る ／ 逆転は `rangeOverflow` ／ in-page fetch で 未来・非日付・逆転・1900 年・PATCH で明日へ が全部 400。PAT spec は `firstDay: 昨日`）。使い捨て spec でライト / ダーク / 390px を目視。docs: features / visualization / data-model / roadmap の「未実装 — A2」を ✅ に。**罠 5 つ**: ① React SSR は input の `name` / `value` を `min` / `max` / `aria-*` の後に出す（テストは id で拾って `toContain`。`checked` が `value` より先、の同族）② Playwright の `getByText` は隠れた要素も数える — ヒント文に summary の語「日を選ぶ」が含まれて strict mode で衝突 → 文言を変え `exact: true` ③ `pnpm e2e` 開始時に `zz-*.spec.ts` があると一緒に走り、共有 sqlite で表示名の断定が落ちる → suite の後に書くか `--grep-invert visual`、断定は `/の庭。/` ④ PATCH で `firstDay: 今日` だけ送ると昨日〜今日の苔片は今日 1 日に縮む正当な編集（400 ではない）— 据え置き意味論の帰結 ⑤ 受領ピルは `position: absolute` で bar の外 → `bar.screenshot()` では写らない、`page.screenshot({ clip })` で）
- 2026-09-06 **B 向き 完了 = merge #46 + 本番デプロイ確認（Workers Builds success、バンドルに「吸う」「インプット」/ `heatmap-lean` / `kind-choices`、CSS に `--in-1` / `.kind-field` / `.post-kind` 到着。実データ目視は残）— migration なし、auto-merge**（`POST` / `PATCH /api/posts` の body に `kind`（3 語 | null | 省略 ＝ 未分類。PATCH は欄の全部を送るので省略で消える。PAT も同じ）、heatmap wire に日ごとの `input` / `output` と窓の `input` / `output`（苔片単位、`total` と同じ数え方）。UI: `KindField`（ネイティブ radio 4 つ、未分類 が戻す手段。積むダイアログは controlled で下書きに退避、編集フォームは uncontrolled で共用）、カードに印（言葉 ＋ 色相の点、両方 は半々）、総草は 入 > 出 青緑 / 出 > 入 赤茶 / 同数・未分類・0 件 緑、凡例に 2 色、読み上げとツールチップに「（インプット a・アウトプット b）」（向きのある日だけ）、キャプションに「吸う x% · 出す y%」（分母は窓の全苔片、向きが無ければ出さない）。配色: OKLCH で `--moss-*` と段ごとに同じ L、h 225（C .11）/ h 55（C .12）— dataviz の `validate_palette` で h 195〜245 × h 15〜60 を総当たり。同じ L に縛るので色相だけの分離は categorical の門に届かない（deutan ΔE 5.8〜7.6、通常視 7.6〜11、`styles.css` に実測）→ 向きは言葉でも運ぶ。単体 336（+19）、e2e 11/11（golden path に input 2 枚 → 読み上げ「4 件（インプット 2・アウトプット 0）」・`in` クラス・比率 40% → 編集で未分類へ戻すと追随。PAT spec の body に `kind`）。使い捨て spec で 14 日分に向きを蒔きライト / ダーク / 390px を目視。docs: visualization / features / data-model の「未実装 — B」を ✅ #46 に、plans §B に実装メモ。**罠 5 つ**: ① React SSR は `checked` を `value` より先に出す（テストの正規表現）② golden path の wire 深い等価（`days.at(-1)`）は列を足すたび壊れる ③ 使い捨て spec の seed は `executeSql` を 1 回に束ねて `test.setTimeout` を伸ばす（1 行 1 回だと 30 秒で死ぬ。`page.evaluate` の中は引数で渡したものしか見えない）④ zsh は `for a in $x` を分割せず `PIPESTATUS` は小文字 `pipestatus` ⑤ `apps/web` からの相対 `git add` が落ち、`;` 区切りの `git push` だけ先に走った（push は同じ `&&` 鎖に））
- 2026-09-06 **A1 の docs 追随 完了 = merge #45（plans §A1-7、docs のみ・auto-merge）**（data-model.md / visualization.md の「A1 までは」「予定」「未実装」を 0004 本番適用済みの文に。migration 節は手書き再構築の実物（`post_tags_keep` 退避・drizzle-kit 0.31 の不正な `ADD NOT NULL`・PRAGMA を書かない理由・`migrations.test.ts` の免除・リハーサル・runbook）、§8 の月バケットは `bucketSpansByMonth` の範囲展開に。残る「未実装」は A2（過去に積む・続く苔片を作る側）と B（`kind` の UI）だけ。arm から 1 分足らずで MERGED。**罠 2 つ**: ① 古い gh には `--json autoMergeRequest` が無い → 公開 REST `pulls/<n>` の `auto_merge.merge_method` を素の curl で読む ② squash merge 後の `git branch -d` は「not fully merged」で拒む → tree の一致を見て `-D`）
- 2026-09-06 **設計 docs 完了 = merge #42**（ADR-0005「苔片が積み上がる軸は「日」の範囲」＋ CONTEXT.md に「続く苔片」「向き」＋ `docs/plans/day-axis-and-kind.md`（A1 → B → A2）＋ features / data-model / visualization / security / roadmap（決めること 10・11 ✅）。grill は 2026-09-06。#43 の後に入れたので status.md の衝突は main の版で解いた。data-model.md / visualization.md の「A1 までは」「予定」「未実装」注記は #43 が入った今は古い → 次の小 PR で直す）
- 2026-09-06 **A1 軸を「日」に 完了 = merge #43 + 本番デプロイ確認（Workers Builds success ＝ `0004` が本番 D1 に適用。merge 前の export は人手で取得済み、事後の COUNT 一致は人手待ち）— 振る舞いを変えない refactor、e2e 11/11 そのまま**（`post` に `first_day` / `last_day`（NOT NULL）と `kind`（NULLABLE）、index `(user_id, first_day, created_at)`。読む側 6 箇所を重なり `first_day <= to AND last_day >= from` の文字列比較へ: 総草は各日 +1 と wire の `total`（マスの合計ではなく窓に重なる苔片数）、年表は MIN(first_day) / MAX(last_day) と months の範囲展開（和 ≧ count）、graph は `last_day >= 初日`、一覧は `(first_day, created_at, id) DESC` でカーソル 3 要素（旧 2 要素は 400）、応答の `day` → `firstDay` / `lastDay` / `postedDay` / `kind`。`to=9999-12-31` の 400 は据え置き。core から読み時の TZ 変換（`dayStartMs` / `bucketByDay` / `bucketByMonth` / `monthKey`）を削除し `bucketSpansByDay`（入 / 出 も）/ `bucketSpansByMonth` / `enumerateMonths` / `monthOf` と `core/kind.ts` を追加。単体 317。**罠 4 つ**: ① drizzle-kit 0.31 は NOT NULL 列の追加を `ALTER TABLE post ADD first_day text NOT NULL` で吐く（SQLite は default 無しの NOT NULL 追加を拒む → 適用時に落ちる）→ 再構築 SQL を手書き（`post_tags_keep` に退避 → `__new_post` へ `date((created_at + 32400000) / 1000, 'unixepoch')` で backfill → DROP / RENAME / index → `INSERT OR IGNORE` で復元）。PRAGMA は書かない（D1 は無視・ローカル SQLite は尊重で挙動が割れる）。`migrations.test.ts` はこの 1 本だけ免除し文の並びを固定。スナップショットは正しいので再 generate は no-op ② migration のリハーサルは `.wrangler/e2e` の写し（e2e の残骸 ＝ 親子行のある fixture）に `--persist-to` で適用 → 行数不変・`first_day` = Intl の Asia/Tokyo。ローカル D1 も `DROP TABLE post` で `post_tags` を空にするので復元経路まで実走できる ③ 設計 docs が未 merge の PR ブランチにあるときは実装を `origin/main` から切り、plans は `git show <branch>:path` で読む。その docs を直す手順は両方 merge 後に ④ 古い gh は `gh pr view --json autoMergeRequest` が無い（実状態は `state,mergedAt,mergeCommit`）。`git push && gh pr merge ; curl` の複合コマンドは permission で止まる → 単独コマンドに分ける）
- 2026-09-05 **書く面のダイアログ化 + 同じ石に積む 完了 = merge #41 + 本番デプロイ確認（Workers Builds success、バンドルに「同じ石に積む」/「見出しを付ける」/「積みました（いまの絞り込みの外）」/ `closedby`、CSS に `dialog.compose` / `.bar-notice.on` 到着。実データ目視は残）— Phase 2 の次の柱を grill-with-docs で決めた回**（候補 5 本 [エクスポート推し / 累積 / タグ運用 / 検索 / サマリー] を比較表にしたが、ユーザの要望で「投稿フォームをダイアログに・トップは振り返りに・苔片からタグを引き継いで書く」に。決定: トップは現順から Composer だけ抜き「積む」は sticky ヘッダー（案 A）／新語 **同じ石に積む**（CONTEXT.md。タグだけ置き換え・本文の下書きは保つ・タグ無しには出ない・元との関係は作らない）／成功で閉じて本文もタグも空（引き継ぎは明示操作だけ — 前は本文だけ空にしてタグを残していた）／一覧に載れば先頭へ scrollIntoView、絞り込みの外ならバー下の `role=status`「積みました（いまの絞り込みの外）」／同乗 = 見出しトグル（決めること 7、`<details>`「見出しを付ける」、畳んでも見出しが summary に出る）+ `n` キー（純粋述語 `isComposeShortcut`）。ADR 無し。実装: `src/Compose.tsx`、`draft.ts` に title（旧形は無しとして読む）、`<datalist>` は Garden に常設（ダイアログは開いている間だけマウント）。**決定は `returnValue` "posted" で運び `onCreated` は close ハンドラから** — ブラウザがフォーカスを invoker に戻した後なので着地のスクロールが打ち消されない。390px は下からのシート。単体 313（+23）+ e2e 11/11（ゴールデンパスに 積む / `n` / Esc 復帰 / 絞り込み外の受領 / 同じ石に積む + 見出し）。同乗の粗 2 件: 年表ピッカーの「足す」2 行折れ（入力欄を縮む側に、ボタンは nowrap）と 390px で見出し「年表」が「年／表」に折れる（`.tl-head` / `.tl-focus` を flex-wrap）。**罠 4 つ**: ① `<dialog>` の UA 既定は content-box — `inline-size: 100vw` + padding で 390px のシートが右にはみ出す → `box-sizing: border-box`（目視でしか出ない）② e2e の 4 spec は **1 つの sqlite を共有**（global-setup のリセットは 1 回、登録は既存 user にパスキーを足す）— ゴールデンパスが見出し付き苔片を残すようになり PAT spec の `WHERE title IS NOT NULL` が 2 行拾った → 自分の行の id で引く（e2e README 詰まったら）③ コンテナの古い gh は `gh pr checks` が pending でも終了コード 1（8 ではない）→ 待ちループは `grep pending` で ④ `sed -n '/a/,/b/p'` で複数ブロックを写すと間の空行が落ちて Edit の old_string が外れる）
- 2026-09-05 **年表の軸ラベル完了 = merge #40 + 本番デプロイ確認（Workers Builds success、バンドルに `ResizeObserver` / `getBoundingClientRect().width` 到着。実データ目視は残）— #38 で見えた粗（390px の多年軸で年ラベルが重なる・1000px でも最後の年が「今日」に重なる）を解消**（`TimelineAxis` が useLayoutEffect + ResizeObserver で svg の幅を測る [初回描画の前に測るので瞬きなし、ピッカー展開で石の列が広がった時も追随]、`axisTicks(domain, axisPx)` は純粋関数のまま: 月境界を全部候補にし [1 月は「YYYY年」]、「今日」に食い込むラベルは退き、暦に揃った刻み（1・3・6 か月 → 1・2・5・10・20・50 年）を隣と重ならなくなるまで粗くする — 「新しい年から数え戻す」間引きは今日の日付で漂うので丸い年（2020, 2025）に揃えた。ラベル幅は字種の見積もり（digit 5.5px / CJK 9px / 隙間 8px @ 9px。描く前に測れないので推定、広めに倒す）、静的 markup は 320px 想定。単体 290 本（+6）+ e2e 11/11、使い捨て spec で 1000px / 390px・ライト / ダーク・4 ドメイン（10 年 / 27 年 / 10 か月 / 2 週）+ ピッカー展開を `getBBox` で実測し全ての隣に隙間あり。visualization.md §8 に 1 項。**罠 3 つ**: ① コンテナの system-ui は DejaVu Sans で digit が 5.7px（SF / Segoe / Roboto / Noto は ~5px）— 字幅の見積もりは spec の `getBBox` で確かめる ② `pkill -f e2e:server` は自分のシェルを殺し（exit 144）、wrangler だけ kill しても workerd が 5183 を握ったまま → `ss -tlnp | grep 5183` の PID と `pgrep -f "wrangler-dist/cli.js dev"` を kill ③ `gh pr create --title` を渡すと main の squash subject は feat のまま（#39 の教訓どおり）。目視で見えた別件の粗: 深掘りピッカーを開くと「足す」が 2 行に折れる）
- 2026-09-05 **総草マスのタップ → その日の投稿一覧完了 = merge #39 + 本番デプロイ確認（Workers Builds success、バンドルに `data-day`、CSS に `outline:2px auto` と `.heatmap{min-width:0}` 到着。実データ目視は残）— 4 本目の導線が投稿一覧に着地し、visualization.md §1 と features.md §3「日」単位ビューが ✅**（マスは石と同じ再構築ボタン [role=button + tabindex、Enter は keydown / Space は keyup]。svg は role="img" → "group"（img の子は presentational でボタンが消える）、軸ラベルは aria-hidden。accessible name は「YYYY/MM/DD · N 件」= チップと同じ綴り（`period.ts` の `slashDay`。53 週の窓には同じ M/D が 2 回現れ得るので年を持つ）、ホバーの `<title>` は「M/D · N 件」のまま。**Tab 停止は今日のマス 1 つ**で矢印キーで歩く（↑↓ = 日、←→ = 週、Home/End = 窓の両端。純粋関数 `walkTarget`、停止はフォーカスに追随し窓の外に出たら今日へ）— 371 マスを全部 Tab 停止にしない。`data-day` 属性で自分のマスを `focus()`（横スクロールも付いてくる）。着地は `Garden` の `showDay`（`showPosts` の期間版: `postPeriod` を `{from: day, to: day}`、`periodKey` が同じなら据え置き → 投稿一覧へ scrollIntoView）— チップ「YYYY/MM/DD ×」で解除、石とは AND、「期間で絞る」の欄にもその日が映る。**既存バグを目視で発見・修正: 総草は一度も「右端 = 今日」で始まっていなかった** — `.heatmaps` は grid で、アイテム `figure.heatmap` の `min-width: auto` が svg の幅（715px）まで伸ばし `.heatmap-scroll` が溢れず（スクロールせず）section ごと画面外へはみ出していた → `min-width: 0`。フォーカスの輪は `outline-style: auto` — solid だと Chromium が SVG 図形の外周を右下へ 2px ずらして描く（3x スクリーンショットで実測）。svg 末尾の余白 +1 → +4（RING）。単体 284 本（+5）+ e2e ゴールデンパスに 1 手（リロード後の Tab 停止 → ↑ で前日へ移り停止も移る → Enter でチップ → 今日のマスは空 → ×）= 11/11。**罠 3 つ**: ① 使い捨て spec の `page.addStyleTag` は本番 CSP（`style-src 'self'`）に弾かれる → CSSOM の `insertRule` で試す ② `pnpm ci` は pnpm 本体の未実装コマンド → `pnpm run ci` ③ handoff commit が同乗した 2 commit のブランチで `gh pr create --fill` すると PR タイトル = ブランチ名になり、main の squash subject が「claude/heatmap day tap (#39)」になった（本文には両 commit の message が残る）→ 次から `--title` を渡す）
- 2026-09-05 **§8 月セグメント棒完了 = merge #38 + 本番デプロイ確認（Workers Builds success、バンドルに `tl-month` / 「活動 」/ `clipPath` 到着。実データ目視は残）— 単棒が歯抜け期間をぶっ通しに見せる問題を解消**（`/api/stats/timeline` の各行に `months: { month: "YYYY-MM", count }[]`（疎・昇順、和 = count）を additive に追加。3 形とも span の MIN/MAX/COUNT SQL は**そのまま**、`created_at` を生で取る 1 文を同じ `db.batch` に足し（1 往復 = 同じスナップショット）、コアの `monthKey` / `bucketByMonth`（`dayKey` の `YYYY-MM`、Asia/Tokyo）で月に束ねる — SQLite の `date()` / `strftime()` は UTC ずれ（9 月 1 日 08:00 JST が 8 月に落ちる）。`?focus=` の生軸は「石を持つ苔片の全リンク」1 文で石の行と石×共起タグの行を同時に賄う（その苔片は石を持つことが構成上保証されるので自己 JOIN 不要）。UI = `monthSegments` が活動月を span で切って `barGeom` に流し（両端は部分月、floor 1.2% はそのまま）、下地 `.tl-span`（fill-opacity .3 = 時代）に活動月 `.tl-month` を実線で重ね、`<clipPath>`（`useId`。React 19.2 の id は `_R_x_` 形式で `url(#…)` にそのまま使える）で下地の角丸に合わせる — 連なりの外側は角丸、連なり同士の切れ目は角ばる = 歯抜けが見える。月をまたぐ棒の aria-label に「活動 N/M か月」、segment に `<title>`、濃淡は付けない。CONTEXT.md に「活動月」。単体 279 本 + e2e 11/11（3 形の months と画面の segment 数）。**目視は使い捨て Playwright spec（`e2e/zz-*.spec.ts` で仮想 authenticator 登録 → in-page fetch で投稿 → `executeSql` で created_at をずらす → section.screenshot、commit 前に消す）で 5 枚** — 既存の粗を観測: 390px の多年軸で年ラベルが重なる（`axisTicks` の間引きは本数基準で幅を見ない）、1000px でも最後の年が「今日」に一部重なる。生軸は (苔片, タグ) リンク 1 行ずつ D1 → Worker に流れる — 日記規模では軽く、重くなったら SQL 側の粗いバケット化が逃げ道）
- 2026-09-05 **期間絞り込み UI 完了 = merge #37 + 本番デプロイ確認（Workers Builds success、バンドルに「期間で絞る」/「期間の絞り込みを外す」到着。実データ目視は残）— Phase 1 の取りこぼしが空になった**（語彙は stats の `?period=month|year|all` [「今の」月/年しか指せない] ではなく、features.md §3 の 日・週・月・年・カスタム範囲を全部含む 1 形 = **総草と同じ `?from=`/`?to=`**（両端含む JST の日、片側だけでも可、`from <= to`、`to=9999-12-31` は 400）に揃え、日・週・月・年はクライアント側プリセット。`resolvePeriod` = `[dayStartMs(from), dayStartMs(addDays(to,1)))` で `resolveWindow` と同じ切り方）。**keyset カーソルとの両立は自明** — 期間はカーソルが歩く `created_at` 軸への gte/lt なので `and(user, tagSetCond, periodCond, cursorCond)` の単純 AND、`?tags=` とも合成。wire 追加（additive）: `today`（サーバが決める JST の今日 = プリセットの起点、年表の `today` と同じ思想）と各苔片の `day`（積まれる「日」。クライアントは zone を持たず比較だけ — 23:59 に置いたチップと 00:01 の投稿の食い違いを解く）。UI = `.feed-filter` に期間チップ（月/年まるごとは「2026年9月」「2026年」と読む）+ `<details>`「期間で絞る」（閉じたままが日常の姿）に 今日 / 今週 / 今月 / 今年（週は総草と同じ日曜始まり・単位まるごと）+ ネイティブ `<input type="date">` 2 欄（`min`/`max` を相互に張り、逆転はブラウザの rangeOverflow で止まり**リクエストにならない**。両方空 = 解除。フォームは `periodKey` で key 付け → 適用した範囲が欄に映る）。単体 = `period.test.ts`（週の年またぎ・閏 2 月・ラベル）+ schema / `resolvePeriod`（JST 境界）= 264 本。e2e ゴールデンパスに 1 手（今日プリセット → **苔片を at rest で 1 日戻す** `UPDATE post SET created_at = created_at - 86400000` [wrangler dev 稼働中の `d1 execute` 書き込みは問題なく通る] → 同日窓が空・前日窓に 1 件・片側だけ・逆転と非日付は 400 → カスタム範囲 → `validity.rangeOverflow` で停止 → チップ × で戻る）= 11/11。Chromium スクリーンショット 4 枚（ライト / ダーク / 390px / `:user-invalid`）で CSS の罠なし。**罠: 前セッションの handoff commit（0d98aaa）が main に載らず local に残っていた → `origin/main` から切った作業ブランチに cherry-pick して同乗（#34/#35 と同じ運び方。セッション開始時に `git log --oneline origin/main..HEAD` で見る）。** 総草マスのタップ → その日の投稿一覧（visualization.md §1）は着地点だけできて配線は未
- 2026-09-04 **本文の Markdown 描画完了 = merge #36 + 本番デプロイ確認（Workers Builds success、バンドルに「Markdown で書けます」/「プレビュー」到着。実データ目視は残）— サニタイザは不採用（[ADR-0004](adr/0004-markdown-renders-to-react-elements.md)）**（tech-stack.md / security.md が想定していた「パーサ ＋ DOMPurify ＋ `dangerouslySetInnerHTML`」から方針変更。security.md の要求は「生 HTML を許さない」＝ サニタイザの仕事は「ほぼ全部落とす」なので、フィルタを 1 枚挟むより**危険な出口を持たない**方が要求に素直。marked は字句解析だけに使い、トークン → React 要素の allowlist に写す ＝ **HTML 文字列を一度も作らない** ＝ サニタイザの取りこぼし [mXSS] が攻撃面にならない。**CSP は無変更**で `script-src 'self'` のまま — `worker/` を 1 行も触っていないことがその証拠）。`dangerouslySetInnerHTML` / `innerHTML` / `outerHTML` が `src/` に無いことを単体テストで固定（コメントは剥がしてから検査 ＝ 散文で言及するのは自由）。**描画の決定**: 見出しは h3〜h6（苔片は投稿一覧 h2 の中）／ `breaks: true`（素描き時代の見え方を保つ — GFM 既定に倒すと既存の苔片が黙って変わる）／ **実体参照は描画器が解く**（marked は素通し ＝ HTML 出力ならブラウザが解く前提。React は文字列をそのまま出すので描画器の仕事。解いた結果もテキストノードなので安全性には効かない）／ 生 HTML はタグにならず字のまま ／ 画像は `<img>` にせずリンク（CSP `img-src 'self' data:` でどのみち表示できず、通せば「日記を開いた時刻」が配信元に漏れる）／ リンクは http・https・mailto と相対のみ、判定は `new URL()` に任せる（`java<TAB>script:` はブラウザと同じ正規化でしか潰せない）、外部は `target="_blank"` ＋ `rel="noopener noreferrer"`。UI = コンポーザと編集フォームが `BodyField` を共有（Markdown 案内 ＋ `<details>` プレビュー。**開いている間だけ描く** ＝ 毎打鍵の字句解析を避け、隠れた本文の写しを DOM に残さない [削除確認ダイアログと同じ理由]。編集の本文は controlled 化）。単体 17 本 ＋ e2e ゴールデンパスに 1 手（本番ビルド + 本番 CSP で、暗号化を往復した本文が要素として描かれ、生 HTML は字・`javascript:` はリンクにならない）= 11/11。**CSS の罠 2 つ（Chromium スクリーンショットの目視でしか出ない）**: ① 苔片は `ol.posts` の中なので `ul` の marker が 2 段目（circle）から始まる ② グローバルの `input {width:100%; min-height:48px}` がチェックリストの印を巨大な枠にする — どちらも `.md` 側で明示指定して解消。**e2e の罠**: 本文を controlled にすると編集フォームを開いた苔片は `hasText` で掴めなくなる（本文が textarea の中へ移る）→ 並び順（新しい順 ＝ 先頭）で掴む
- 2026-09-04 **`deleted_at` の drop 完了 = merge #35 + 本番 D1 適用確認 — ADR-0003 の後始末が着地し、スキーマから論理削除の痕跡が消えた**（生成 SQL は `DROP INDEX post_deleted_at_idx` → `ALTER TABLE post DROP COLUMN deleted_at` の 2 文だけ。`CREATE TABLE` / `DROP TABLE` / `__new_` / `PRAGMA foreign_keys=OFF` はどれも無し ＝ **`post` の再構築なし** ＝ D1 が PRAGMA を無視して `post_tags` を CASCADE 全消しする罠の対象外。SQLite は列 drop を再構築せずその場で行うが索引付きの列は落とせない → `DROP INDEX` が先である必要）。**検証: 空 DB では CASCADE の有無が観測できない**ので、ローカル D1 に user/post/tag/post_tags を親子 1 行ずつ仕込んでから適用 — 4 テーブルとも 1 行のまま・`post` の CREATE 文から `deleted_at` だけ消え FK `ON DELETE cascade` は残存・`post_deleted_at_idx` のみ消滅・`SELECT deleted_at` は "no such column"。`migrations.test.ts` に 0003 の文を並びごと固定（3 文目が生えたら失敗 ＝ drizzle-kit が再構築に切り替わった合図）、`schema.ts` の規約 1 を「ADDITIVE」→**「再構築しない」**に言い直し列 drop を安全な形として明記。**新しい確認経路: クライアントコードを変えない PR は従来のバンドルプローブが使えない → 公開リポの check-runs を素の curl で読むと `Workers Builds: kokemusu` の成否が見える**（`curl -H 'Accept: application/vnd.github+json' https://api.github.com/repos/okayus/kokemusu/commits/<sha>/check-runs`。トークン不要で `gh api` の deny も回避。deploy command は `migrations apply --remote && deploy` の `&&` 連結なので success ＝ 本番 D1 への適用成功と読める）。**罠: `gh pr merge` の「error connecting to api.github.com」は本物の失敗（未 merge、リトライで成立）** — #30 の GraphQL エラーは表示だけで merge は成功していたので、**エラー表示は両方向に当てにならず `gh pr view --json state,mergedAt` が唯一の判定**
- 2026-09-04 **投稿の編集・削除完了 = ADR-0003「削除は物理削除」+ merge #34 + 本番デプロイ確認（プローブ: バンドルに削除確認ダイアログ到着。実データ目視は残）— ソフトデリートは調査の結果廃止**（復元設計の不在・security.md「平文が D1・Time Travel・バックアップに残る期間を作らない」と逆行・全読みクエリへの `isNull` 税。誤削除の受け皿 = 確認ダイアログ → エクスポート → D1 Time Travel [Paid 30d / Free 7d、常時有効] → 定期バックアップ。「隠す」需要は `tag.archived_at` の前例で状態として別モデル化）— PATCH /api/posts/:id = 本文/見出し再暗号化 + `updatedAt`、タグは 1 batch 総入れ替え（旧リンク削除 → 新規タグ → 新リンク。タグ解決は `resolveTagRows` で create と共通化）／ DELETE = 物理 1 文（post_tags は `ON DELETE CASCADE` — D1 は FK enforcement を切れない）。両方 requireSession のみ・未知/他人 id は 404（tokens の流儀）。読み経路の `isNull(deletedAt)` 7 箇所を除去、`deleted_at` 列は「書かない列」で残し drop は後続 migration PR（DROP INDEX → DROP COLUMN）。UI = 苔片に編集（コンポーザ鏡写しのインライン）/ 削除（native `<dialog>` + 本文プレビュー、`closedby=any` は PE。**確認 dialog は確認中だけマウント — 常設だと隠れた本文コピーが DOM に残り Playwright strict mode が検出**）。e2e = 編集の再暗号化 at rest + 削除の一覧/総草/COUNT/CASCADE 検査 = 11/11。**罠: handoff ブランチ同乗の pre-squash 履歴が main の #33 squash と分岐 → PR が CONFLICTING で CI 不起動。origin/main をマージ（全 ours、`git diff --cached HEAD` 空 = ツリー一致を検算）して解消 — 以後、作業ブランチは main の squash 後から切る**
- 2026-09-03 **タグ絞り込み UI 完了 = merge #33 + 本番デプロイ確認（プローブ: バンドルに tg-bridge /「投稿一覧へ」到着。実データの本番目視は §6 と合わせて残）— Phase 1 取りこぼし筆頭を消化、3 導線が投稿一覧に着地** — `GET /api/posts` に `?tags=<id>,<id>`（タグ集合 AND・2..20・`?tag=` と排他・未知 id は空。SQL は年表 `?tags=` と同形の `HAVING COUNT(DISTINCT tag_id) = n` を WHERE に合流 = keyset カーソルとそのまま両立。パーサ `parseTagsParam` と上限 `MAX_TAGS_PER_POST` は core/tag.ts へ移して stats/posts で共有）。UI = 投稿一覧を section 化（h2 + 絞り込みバー: チップタップで 1 石ずつ外す / 解除、visually-hidden の role=status が変更を読み上げ）。導線 = ① 苔片のタグチップをボタン化（1 石・`?tag=` 名前）② §8 フォーカスヘッダに「投稿一覧へ」③ §6 の橋を role=button 化（石と同じ Enter/Space 契約 + 透明の太ヒットライン）→ 両タグ AND。filter 切替は adjust-state-while-rendering + epoch ref（遅延した「もっと遡る」応答の混入防止）、絞り込み中に合わない苔片は一覧に差し込まない（総草の濃化が受領の証）。e2e ゴールデンパスに 1 手（2 本目の苔片で絞り込みを観測可能に → 3 導線 + `?tag=`/`?tags=` 両 wire 形 = 11/11。橋ボタン追加で石の getByRole が曖昧化 → `exact: true`）。roadmap の残りからタグ絞り込みを消し（期間絞り込みは残）、visualization.md 優先順位 3 の「エッジタップ未」注記を解消
- 2026-09-03 **縦切り「§6 タグ関係グラフ」完了 = merge #32 + 本番デプロイ確認（プローブ: 本番バンドルに「石のつながり」到着。実データの本番目視は残）— §8 → PAT → §6 の縦切り列を完走、可視化の柱 3 本（総草・年表・つながり）が本番稼働** — `GET /api/stats/graph?period=month|year|all`（既定 = 全期間。境界は core `dayKey` の JST 月初/年初、ノード = タグ別件数・エッジ = `post_tags` 自己 JOIN `a.tag_id < b.tag_id`、D1 batch 1 往復、wire は count 降順）。UI = 自作 SVG「石のつながり」+ 乱数なしの決定的力学レイアウト（フィロタキシス初期配置 → 表面間距離の斥力・橋バネ [クリアランス 12px で解放]・中央重力 → 枠中央へ平行移動。Chromium スクリーンショット 6 枚で目視調整、markup/不変条件は vitest で固定）。石 = 固定物差し √件数（min 7 / max 30、ヒートマップの level と同思想）、橋の太さ = 共起回数。ノードタップの着地 = §8 フォーカス（TagTimeline の focus を Garden に持ち上げ controlled 化 + CSS `scroll-behavior` 経由 scrollIntoView = reduced-motion 尊重）。エッジタップの投稿一覧行きは**未** — `?tag=` は API/クライアント実装済みで絞り込み UI だけが無い（Phase 1 取りこぼし筆頭）。期間 = fieldset + ネイティブ radio、SVG の石 = role=button + Enter(keydown)/Space(keyup) の native 契約。e2e ゴールデンパスに 1 手（2 石 1 橋 → 石タップで内訳年表 → wire で a<b ペアと `?period=month`）= 11/11。**罠: drizzle + D1 の `batch()` はオブジェクト行しか返せず、自己 JOIN で出力列名が重複（両側 `tag_id`）するとキーが潰れて位置マッピングが崩壊 → select に明示 `.as()` 必須**（単体は D1 無しで不可視、e2e の wire 検査が検出。stats.ts にコメントで記録 → skill 書き戻し候補）。visualization.md 優先順位 3 に ✅
- 2026-09-03 **人手作業完了: 本番 `PAT_PEPPER` を設定（1Password に控え済み）し最初の PAT を発行 — 受け側は本番稼働状態に**（発行が 201 で通る = pepper 設定後の発行、という順序保証が fail-closed 設計の狙いどおり効いた）。残りは送り側 = mazuoboeru の日次 push 実装（別リポ。wire 契約 = `POST /api/posts`、Bearer + content-type のみ・Origin 不要・リトライなし[Idempotency-Key 未実装]・tags に `"mazuoboeru"`、境界は throw しない/status だけログ — 貼り付け用プロンプトをセッションで手渡し済み）。`KOKEMUSU_PAT` secret と `KOKEMUSU_URL` var は **mazuoboeru 側**の wrangler に置く（kokemusu の wrangler.jsonc に誤って足された KOKEMUSU_URL は revert）
- 2026-09-03 **縦切り「PAT（API 自動投稿の受け側）」完了 = merge #31 + 本番デプロイ確認**（プローブ: junk Bearer POST が 403→401 に反転 = Bearer 免除入りコード + migration `0002_api_token` 適用） — `api_token` 葉テーブル（CREATE TABLE + index 2 本のみ = 再構築なし、`migrations.test.ts` でも担保）。検証順 = `Bearer ` → 接頭辞 `kokemusu_pat_` → pepper を文字列比較で弾いてから sha256+D1 1 点読み。PAT は Cookie より先に判定、CSRF は Bearer の存在で免除。**PAT が届くのは `POST /api/posts`（post:write）と whoami 用 `GET /api/auth/me` だけ**（他は 403 `session_required` — write-only token が漏れても日記は読めない、PAT で PAT は作れない）。`PAT_PEPPER` は fail closed（未設定 = 発行 503 / 検証全不一致。skill の `?? ""` を意図的に締めて「pepper 未設定で発行 → 後から設定して全滅」を構造排除）。`API_RATE_LIMITER`（1002、120/60s）をログイン用と別枠で追加。設定 UI = 発行 → 一度きり表示（user-select:all + コピー）→ 一覧 → 失効（行は「失効済み」で残す・他人の id は 404）。e2e 4 本目 `pat.spec.ts`（UI 発行 → Cookie 無し Bearer 201 → `k1.` 封筒 → write-only wall → 同乗 Cookie より PAT 優先 → UI 失効で 401 = 11/11 ×2）。skill `cloudflare-workers-pat-bearer-auth` の passkey-session 変種を初適用（UNVERIFIED 2 件が検証済みに → 書き戻し待ち）。罠: 前セッションの `e2e:server` 残存を `reuseExistingServer` が掴み古いビルドに向かう → e2e README 詰まり表に追記
- 2026-09-03 **縦切り「§8 タグのタイムライン」完了 = DoD 1〜5 全通過（merge #30 + 本番目視 OK）** — `GET /api/stats/timeline` 3 形（既定 = 全タグの行を開始日順 / `?focus=` = 石のみ + 石×共起タグの自己 JOIN を D1 batch 1 往復 / `?tags=` = タグ集合 AND の 1 行 `HAVING COUNT(DISTINCT tag_id) = n`）。first/last は core `dayKey`（Asia/Tokyo）で日に畳む — BODY_KEY・SQL `date()` ともこの経路に無い。自作 SVG 横棒年表（subgrid 3 列 = 石チップ | % 座標の単棒 | 件数·密度（期間÷件数）、月/年 tick + 今日、light-dark()、markup テストで構造固定）、フォーカス = 石チップのタップで内訳年表 + 行の ＋ で 3 タグ以上の ad-hoc 深掘り（保存なし・× で派生行ごと除去）。e2e ゴールデンパスに 1 手（2 石の行 → 石タップで共起行 → `?tags=` の wire 検査 = 3 形とも実 sqlite 経由で green）。visualization.md 優先順位 2 に ✅、plans/tag-timeline.md 削除
- 2026-09-03 **設計確定 merge #28: 案件はタグで表す — §8 タイムラインは「行 = タグ集合 AND の単棒」+ フォーカス年表** — 見出し基準は不成立（AES-GCM のランダム IV で同じ平文でも毎回別暗号文 = SQL の GROUP BY 不可。全件復号は BODY_KEY を stats に入れることになる）、ADR-0001 無変更。フラットタグのまま（kind は NULLABLE 後付け可・色/絵文字で目印）、タグ名/説明に顧客固有名を書かない（security.md）、フォーカス = 石タップで共起タグの行が自動 + チップで 3 タグ以上に深掘り（例: XX案件×Vue.js×Hono）、月セグメント棒は後続強化、優先順位 §8↔§6 入替。次の縦切り = §8（DoD は plans/tag-timeline.md、並びは §8 → PAT → §6）
- 2026-09-02 **PR6 merge #26: e2e 3 spec をコンテナ内で** — Chromium をイメージに焼き込み（playwright 1.62.1、`INSTALL_PLAYWRIGHT` / `PLAYWRIGHT_VERSION`、ホストで rebuild 済み）。ビルド成果物を `wrangler dev --config dist/kokemusu/wrangler.json --persist-to .wrangler/e2e --ip 127.0.0.1` で配信し、`e2e/prepare-config.ts` が派生 config から ratelimits を外し・vars を e2e 用に差し替え・ビルドが `dist/` にコピーする `.dev.vars` を消す。ゴールデンパス（仮想 authenticator 登録 → 投稿 → 総草の今日が l0→l1 → D1 の `k1.` 封筒 → リロード → ログアウト → ログイン）/ 401 境界 / セキュリティヘッダ = 10 tests green ×2。CI は `tsconfig.e2e.json` の型検査だけ（e2e は merge 前にコンテナ内で）。罠: wrangler dev の proxy は「body 未読で応答したリクエストの次」を 500 にして exit（4.125 / 4.128 実測、本番は無関係 → spec 側の規約、`apps/web/e2e/README.md`）
- 2026-09-02 人手待ち 2 件を解消: Chromium 焼き込み（上記）+ スキル書き戻し okayus-skills#41（e2e 0.4.0 / sandbox 0.2.0 / passkey 0.2.1 — `.dev.vars` コピー・Trap 3・専用 state dir・**v4 `ratelimits` は `wrangler dev --config` でもローカル実動**（40 連発 = 29×200 + 11×429）・`--var` > `.dev.vars` > `vars` の優先順位・secret put パイプ罠）。passkey の UNVERIFIED 3 件は matatabetai 経由で 0.2.0 時点で解消済みだった
- 2026-09-02 **縦切り「投稿 → タグ → ヒートマップに 1 マス点く」完了 = DoD 1〜5 全通過**（DoD 4 = 総草で今日のマスが 1 段濃くなるのを本番目視、DoD 5 = 本番 D1 の `substr(body,1,20)` が `k1.XRHdTggcBi-M5-a6.` — 封筒の `k1.` + IV16 文字 + `.` でちょうど 20 文字 = 平文ゼロ）。plans/vertical-slice.md を削除、PR6（e2e）は status の次の 3 手へ
- 2026-09-02 方向転換 merge #25: **ヒートマップは総草 1 枚だけ**（タグ別を廃止、API の `?tag=` も削除）。タグへの打ち込みは「量 = タグ関係グラフのノード成長（visualization.md §6）」「期間 = タグのタイムライン（§8 新設、最初〜最後の苔片の横棒年表）」で見る。用語「総草」を CONTEXT.md へ。ADR 変更なし — 0001 の「平文メタデータで集計」はグラフ/タイムラインでも同じ根拠で成立
- 2026-09-02 縦切り PR5 後半 merge #24: `GET /api/stats/heatmap`（半開き窓・既定「今日を含む 53 週」= to の週の日曜から 52 週遡る 53 列・**濃淡は固定閾値 `level = min(count,4)` をサーバが返す** — タグ間で同じ濃さ = 同じ件数・過去のマスが他の日の増減で動かない・1 投稿で確実に 1 段。本文に触らないので BODY_KEY ゲート無し）+ 自作 SVG（週=列/曜日=行・日曜始まり・`--moss-0..4` light-dark() トークン、dark は「多いほど明るい」へ別途選段し OKLCH 単調性を機械検証・右端 = 今日の横スクロール・格子は renderToStaticMarkup の markup テストで固定）。day.ts に `dayOfWeek`（0=日曜）追加
- 2026-09-02 縦切り PR5 前半 merge: 日の軸 `worker/core/day.ts`（#22）— 苔片がどのマスに落ちるかを Intl でゾーンから決める純粋関数（SQLite の `date()` は UTC 基準なので朝 9 時前が前日にずれる）。`dayKey` / `bucketByDay` に加えて逆関数 `dayStartMs` と暦の `addDays` / `enumerateDays`、境界テスト 28 件。**Temporal は workerd 2026-08-20 にも TS 7.0.2 にも無い**ことを実測（来たら `dayKey` / `dayStartMs` の 2 本体だけで置き換わる）。代わりに Temporal をオラクルにした差分検証 13 ゾーン 53k 値で不一致ゼロ
- 2026-09-01 縦切り PR4 merge: 投稿 API + 最小タイムライン（#20。post + 新規 tag + post_tags を原子的 db.batch・タグは norm で dedupe・keyset カーソル・`/api/*` に no-store・⌘/Ctrl+Enter と localStorage 退避の 1 欄コンポーザ）。人手の BODY_KEY 設定と本番投稿の動作確認まで完了 = **DoD 1〜3 通過**、D1 に平文ゼロはローカル実証（本番の封筒目視 = DoD 5 は次の 3 手）
- 2026-09-01 縦切り PR3 merge: 本文暗号化コア（#19、ADR-0001）— AES-256-GCM・封筒 `k1.<iv>.<暗号文>`・鍵は引数で受ける純粋関数のみ・BODY_KEY 未設定/不正は fail closed
- 2026-09-01 PR2 の本番検証完了: 端末 2 台のパスキー登録・ログイン往復・`INITIAL_REGISTRATION_TOKEN` 削除で登録の扉を閉じた（外形も実測: 未認証 `me` 401 / `register/begin` 403 / `login/begin` 200）。**RP_ID = kokemusu.shiraoka.workers.dev はこれで確定**。secret put のパイプ形は値が画面に出ない罠 → 2 段形に訂正（#14 コメント）
- 2026-08-29 merge を `gh pr merge --auto --squash` の opt-in へ（例外あり、CLAUDE.md）、ci.yml を安定シェル化（`.node-version` / root `ci` script / Dependabot、#16）。matatabetai（ADR-001 改訂 2026-08-24）と okayus-skills token skill 0.2.4 に追随。5 日間 merge 待ちだった #15 は 2026-08-29 にホストで merge
- 2026-08-29 本番だけ verify 系が全部 500 → hotfix #15: hono は `__Host-` cookie の**削除**にも secure を要求して throw する（`consumeChallenge` の deleteCookie が漏れ。http のローカル / 単体テストは素の cookie 名で発火しない）。削除を `clearCookie()` に一元化し、https ORIGIN の回帰テスト 3 件を追加。skill passkey-auth の参照コードにも同罠 = 書き戻し対象
- 2026-08-24 縦切り PR2 merge: パスキー認証 single-user 変種（`user` 行が既にあれば credential を足すだけ = 苔片を孤児にしない）+ secureHeaders / CSP（vite dev の HMR とは `DEV_CSP=1` で分離）+ CSRF Origin 検査 + ratelimits / observability + 最小ログイン UI + Node 単体テスト 38 件、ローカル ORIGIN を 5273 に修正（#14。skill の UNVERIFIED 3 件を実物で解消、rate limiter は現行版ならローカル実動 = 書き戻しメモは PR 本文）
- 2026-08-24 Workers Builds の deploy command が本番マイグレを当てることを実証（#11 merge 直後、ホストの `wrangler d1 migrations list --remote` = 未適用 0 件）。以後マイグレを含む PR は merge するだけでよい（#12、dev-environment.md §3 に恒久記録）
- 2026-08-24 縦切り PR1 merge: 6 テーブル（user/credential/session/post/tag/post_tags）の実スキーマ + Drizzle 配線（ルート無し）、additive 検査 migrations.test.ts を CI に。0000_init との連番衝突は 0001 へずらして解決（#11、skill cloudflare-d1-drizzle-migration）
- 2026-08-23 push 起動の Workers Builds を実証（`main` の #7 / #8 / #9 の各 commit に check-run `Workers Builds: kokemusu` が success）。Phase 0 の残件が消えた
- 2026-08-23 対話シェルを `op run` で包むのをやめ（TTY が壊れる）、`shell.sh` に TTY 判定と `op://` パーサの堅牢化、`.claude/settings.json` の deny の穴（refspec 形の push / ブランチ削除）を塞ぐ（#8 / #9）
- 2026-08-23 最初の縦切り「投稿 → タグ → ヒートマップに 1 マス点く」を PR1〜6 に分割（`docs/plans/vertical-slice.md`）。決めること 4（UI トーン）・6（タグ構造）・9（集計の「日」）を確定、`RP_ID = kokemusu.shiraoka.workers.dev` を永久固定、GitHub token を `./shell.sh` の exec 時シェルへ移動（#7）
- 2026-08-23 進捗管理を status hub 化: `docs/status.md`（40 行上限）+ `docs/log.md` + SessionStart hook + `/handoff` + CI の上限検査。CLAUDE.md を規約のみに圧縮（#6）
- 2026-08-23 Workers Builds 接続完了。本番 `/health` 200・SPA 配信・`0000_init` 適用。My Profile で作った手作りトークンは picker に出ず「新しいトークンを作成する」で生成（skill `cloudflare-workers-builds-keyless-deploy` 0.3.0 = okayus-skills #24）。push 起動のビルドは未実証
- 2026-08-23 #3（docs drift）・#5（grill: ADR-0001 本文暗号化 / ADR-0002 受け側 PAT / `CONTEXT.md`）・#4（歩く骨格 `apps/web`、CI を typecheck/build/test 化、`database_id` 実 UUID）を merge
- 2026-08-22 サンドボックス再同期（node:24）・1 リポ限定 PAT を 1Password で注入（`./up.sh`）・MCP（cloudflare-docs / context7）・docs egress 10 ドメイン・`modern-web-guidance` 同梱・CI placeholder・`main` ruleset。E2E は #1、docs は #2
- 2026-06-05 デプロイ方式 = Cloudflare Workers + D1 に決定（roadmap「決めること」1）
