# ログ（追記専用・新しい順）

1 行 = 1 節目（PR の merge・ADR・人手作業の完了・本番の状態変化）。`- YYYY-MM-DD 何を（#PR / ADR / skill）`。
自動ロードはされない。必要なら `head -20 docs/log.md`。作業中の試行錯誤は書かない（git log と PR にある）。

- 2026-09-03 **縦切り「§6 タグ関係グラフ」完了 = merge #32 + 本番デプロイ確認（プローブ: 本番バンドルに「石のつながり」到着。実データの本番目視は残）— §8 → PAT → §6 の縦切り列を完走、可視化の柱 3 本（総草・年表・つながり）が本番稼働** — `GET /api/stats/graph?period=month|year|all`（既定 = 全期間。境界は core `dayKey` の JST 月初/年初、ノード = タグ別件数・エッジ = `post_tags` 自己 JOIN `a.tag_id < b.tag_id`、D1 batch 1 往復、wire は count 降順）。UI = 自作 SVG「石のつながり」+ 乱数なしの決定的力学レイアウト（フィロタキシス初期配置 → 表面間距離の斥力・橋バネ [クリアランス 12px で解放]・中央重力 → 枠中央へ平行移動。Chromium スクリーンショット 6 枚で目視調整、markup/不変条件は vitest で固定）。石 = 固定物差し √件数（min 7 / max 30、ヒートマップの level と同思想）、橋の太さ = 共起回数。ノードタップの着地 = §8 フォーカス（TagTimeline の focus を Garden に持ち上げ controlled 化 + CSS `scroll-behavior` 経由 scrollIntoView = reduced-motion 尊重）。エッジタップの投稿一覧行きは**未** — `?tag=` は API/クライアント実装済みで絞り込み UI だけが無い（Phase 1 取りこぼし筆頭）。期間 = fieldset + ネイティブ radio、SVG の石 = role=button + Enter(keydown)/Space(keyup) の native 契約。e2e ゴールデンパスに 1 手（2 石 1 橋 → 石タップで内訳年表 → wire で a<b ペアと `?period=month`）= 11/11。**罠: drizzle + D1 の `batch()` はオブジェクト行しか返せず、自己 JOIN で出力列名が重複（両側 `tag_id`）するとキーが潰れて位置マッピングが崩壊 → select に明示 `.as()` 必須**（単体は D1 無しで不可視、e2e の wire 検査が検出。stats.ts にコメントで記録 → skill 書き戻し候補）。visualization.md 優先順位 3 に ✅
- 2026-09-03 **人手作業完了: 本番 `PAT_PEPPER` を設定（1Password に控え済み）し最初の PAT を発行 — 受け側は本番稼働状態に**（発行が 201 で通る = pepper 設定後の発行、という順序保証が fail-closed 設計の狙いどおり効いた）。残りは送り側 = mazuoboeru の日次 push 実装（別リポ。wire 契約 = `POST /api/posts`、Bearer + content-type のみ・Origin 不要・リトライなし[Idempotency-Key 未実装]・tags に `"mazuoboeru"`、境界は throw しない/status だけログ — 貼り付け用プロンプトをセッションで手渡し済み）。`KOKEMUSU_PAT` secret と `KOKEMUSU_URL` var は **mazuoboeru 側**の wrangler に置く（kokemusu の wrangler.jsonc に誤って足された KOKEMUSU_URL は revert）
- 2026-09-03 **縦切り「PAT（API 自動投稿の受け側）」完了 = merge #31 + 本番デプロイ確認**（プローブ: junk Bearer POST が 403→401 に反転 = Bearer 免除入りコード + migration `0002_api_token` 適用） — `api_token` 葉テーブル（CREATE TABLE + index 2 本のみ = 再構築なし、`migrations.test.ts` でも担保）。検証順 = `Bearer ` → 接頭辞 `kokemusu_pat_` → pepper を文字列比較で弾いてから sha256+D1 1 点読み。PAT は Cookie より先に判定、CSRF は Bearer の存在で免除。**PAT が届くのは `POST /api/posts`（post:write）と whoami 用 `GET /api/auth/me` だけ**（他は 403 `session_required` — write-only token が漏れても日記は読めない、PAT で PAT は作れない）。`PAT_PEPPER` は fail closed（未設定 = 発行 503 / 検証全不一致。skill の `?? ""` を意図的に締めて「pepper 未設定で発行 → 後から設定して全滅」を構造排除）。`API_RATE_LIMITER`（1002、120/60s）をログイン用と別枠で追加。設定 UI = 発行 → 一度きり表示（user-select:all + コピー）→ 一覧 → 失効（行は「失効済み」で残す・他人の id は 404）。e2e 4 本目 `pat.spec.ts`（UI 発行 → Cookie 無し Bearer 201 → `k1.` 封筒 → write-only wall → 同乗 Cookie より PAT 優先 → UI 失効で 401 = 11/11 ×2）。skill `cloudflare-workers-pat-bearer-auth` の passkey-session 変種を初適用（UNVERIFIED 2 件が検証済みに → 書き戻し待ち）。罠: 前セッションの `e2e:server` 残存を `reuseExistingServer` が掴み古いビルドに向かう → e2e README 詰まり表に追記
- 2026-09-03 **縦切り「§8 タグのタイムライン」完了 = DoD 1〜5 全通過（merge #30 + 本番目視 OK）** — `GET /api/stats/timeline` 3 形（既定 = 全タグの行を開始日順 / `?focus=` = 石のみ + 石×共起タグの自己 JOIN を D1 batch 1 往復 / `?tags=` = タグ集合 AND の 1 行 `HAVING COUNT(DISTINCT tag_id) = n`）。first/last は core `dayKey`（Asia/Tokyo）で日に畳む — BODY_KEY・SQL `date()` ともこの経路に無い。自作 SVG 横棒年表（subgrid 3 列 = 石チップ | % 座標の単棒 | 件数·密度（期間÷件数）、月/年 tick + 今日、light-dark()、markup テストで構造固定）、フォーカス = 石チップのタップで内訳年表 + 行の ＋ で 3 タグ以上の ad-hoc 深掘り（保存なし・× で派生行ごと除去）。e2e ゴールデンパスに 1 手（2 石の行 → 石タップで共起行 → `?tags=` の wire 検査 = 3 形とも実 sqlite 経由で green）。visualization.md 優先順位 2 に ✅、plans/tag-timeline.md 削除
- 2026-09-03 **設計確定 merge #28: 案件はタグで表す — §8 タイムラインは「行 = タグ集合 AND の単棒」+ フォーカス年表** — 見出し基準は不成立（AES-GCM のランダム IV で同じ平文でも毎回別暗号文 = SQL の GROUP BY 不可。全件復号は BODY_KEY を stats に入れることになる）、ADR-0001 無変更。フラットタグのまま（kind は NULLABLE 後付け可・色/絵文字で目印）、タグ名/説明に顧客固有名を書かない（security.md）、フォーカス = 石タップで共起タグの行が自動 + チップで 3 タグ以上に深掘り（例: XX案件×Vue.js×Hono）、月セグメント棒は後続強化、優先順位 §8↔§6 入替。次の縦切り = §8（DoD は plans/tag-timeline.md、並びは §8 → PAT → §6）
- 2026-09-02 **PR6 merge #26: e2e 3 spec をコンテナ内で** — Chromium をイメージに焼き込み（playwright 1.62.1、`INSTALL_PLAYWRIGHT` / `PLAYWRIGHT_VERSION`、ホストで rebuild 済み）。ビルド成果物を `wrangler dev --config dist/kokemusu/wrangler.json --persist-to .wrangler/e2e --ip 127.0.0.1` で配信し、`e2e/prepare-config.ts` が派生 config から ratelimits を外し・vars を e2e 用に差し替え・ビルドが `dist/` にコピーする `.dev.vars` を消す。ゴールデンパス（仮想 authenticator 登録 → 投稿 → 総草の今日が l0→l1 → D1 の `k1.` 封筒 → リロード → ログアウト → ログイン）/ 401 境界 / セキュリティヘッダ = 10 tests green ×2。CI は `tsconfig.e2e.json` の型検査だけ（e2e は merge 前にコンテナ内で）。罠: wrangler dev の proxy は「body 未読で応答したリクエストの次」を 500 にして exit（4.125 / 4.128 実測、本番は無関係 → spec 側の規約、`apps/web/e2e/README.md`）
- 2026-09-02 人手待ち 2 件を解消: Chromium 焼き込み（上記）+ スキル書き戻し okayus-skills#41（e2e 0.4.0 / sandbox 0.2.0 / passkey 0.2.1 — `.dev.vars` コピー・Trap 3・専用 state dir・**v4 `ratelimits` は `wrangler dev --config` でもローカル実動**（40 連発 = 29×200 + 11×429）・`--var` > `.dev.vars` > `vars` の優先順位・secret put パイプ罠）。passkey の UNVERIFIED 3 件は matatabetai 経由で 0.2.0 時点で解消済みだった
- 2026-09-02 **縦切り「投稿 → タグ → ヒートマップに 1 マス点く」完了 = DoD 1〜5 全通過**（DoD 4 = 総草で今日のマスが 1 段濃くなるのを本番目視、DoD 5 = 本番 D1 の `substr(body,1,20)` が `k1.XRHdTggcBi-M5-a6.` — 封筒の `k1.` + IV16 文字 + `.` でちょうど 20 文字 = 平文ゼロ）。plans/vertical-slice.md を削除、PR6（e2e）は status の次の 3 手へ
- 2026-09-02 方向転換 merge #25: **ヒートマップは総草 1 枚だけ**（タグ別を廃止、API の `?tag=` も削除）。タグへの打ち込みは「量 = タグ関係グラフのノード成長（visualization.md §6）」「期間 = タグのタイムライン（§8 新設、最初〜最後の苔片の横棒年表）」で見る。用語「総草」を CONTEXT.md へ。ADR 変更なし — 0001 の「平文メタデータで集計」はグラフ/タイムラインでも同じ根拠で成立
- 2026-09-02 縦切り PR5 後半 merge #24: `GET /api/stats/heatmap`（半開き窓・既定「今日を含む 53 週」= to の週の日曜から 52 週遡る 53 列・**濃淡は固定閾値 `level = min(count,4)` をサーバが返す** — タグ間で同じ濃さ = 同じ件数・過去のマスが他の日の増減で動かない・1 投稿で確実に 1 段。本文に触らないので BODY_KEY ゲート無し）+ 自作 SVG（週=列/曜日=行・日曜始まり・`--moss-0..4` light-dark() トークン、dark は「多いほど明るい」へ別途選段し OKLCH 単調性を機械検証・右端 = 今日の横スクロール・格子は renderToStaticMarkup の markup テストで固定）。day.ts に `dayOfWeek`（0=日曜）追加
- 2026-09-02 縦切り PR5 前半 merge: 日の軸 `worker/core/day.ts`（#22）— 苔片がどのマスに落ちるかを Intl でゾーンから決める純粋関数（SQLite の `date()` は UTC 基準なので朝 9 時前が前日にずれる）。`dayKey` / `bucketByDay` に加えて逆関数 `dayStartMs` と暦の `addDays` / `enumerateDays`、境界テスト 28 件。**Temporal は workerd 2026-08-20 にも TS 7.0.2 にも無い**ことを実測（来たら `dayKey` / `dayStartMs` の 2 本体だけで置き換わる）。代わりに Temporal をオラクルにした差分検証 13 ゾーン 53k 値で不一致ゼロ
- 2026-09-01 縦切り PR4 merge: 投稿 API + 最小タイムライン（#20。post + 新規 tag + post_tags を原子的 db.batch・タグは norm で dedupe・keyset カーソル・`/api/*` に no-store・⌘/Ctrl+Enter と localStorage 退避の 1 欄コンポーザ）。人手の BODY_KEY 設定と本番投稿の動作確認まで完了 = **DoD 1〜3 通過**、D1 に平文ゼロはローカル実証（本番の封筒目視 = DoD 5 は次の 3 手）
- 2026-09-01 縦切り PR3 merge: 本文暗号化コア（#19、ADR-0001）— AES-256-GCM・封筒 `k1.<iv>.<暗号文>`・鍵は引数で受ける純粋関数のみ・BODY_KEY 未設定/不正は fail closed
- 2026-09-01 PR2 の本番検証完了: 端末 2 台のパスキー登録・ログイン往復・`INITIAL_REGISTRATION_TOKEN` 削除で登録の扉を閉じた（外形も実測: 未認証 `me` 401 / `register/begin` 403 / `login/begin` 200）。**RP_ID = kokemusu.shiraoka.workers.dev はこれで確定**。secret put のパイプ形は値が画面に出ない罠 → 2 段形に訂正（#14 コメント）
- 2026-08-29 merge を `gh pr merge --auto --squash` の opt-in へ（例外あり、CLAUDE.md）、ci.yml を安定シェル化（`.node-version` / root `ci` script / Dependabot、#16）。matatabetai（ADR-001 改訂 2026-08-24）と okayus-skills token skill 0.2.4 に追随。5 日間 merge 待ちだった #15 は 2026-08-29 にホストで merge
- 2026-08-29 本番だけ verify 系が全部 500 → hotfix #15: hono は `__Host-` cookie の**削除**にも secure を要求して throw する（`consumeChallenge` の deleteCookie が漏れ。http のローカル / 単体テストは素の cookie 名で発火しない）。削除を `clearCookie()` に一元化し、https ORIGIN の回帰テスト 3 件を追加。skill passkey-auth の参照コードにも同罠 = 書き戻し対象
- 2026-08-24 縦切り PR2 merge: パスキー認証 single-user 変種（`user` 行が既にあれば credential を足すだけ = 苔片を孤児にしない）+ secureHeaders / CSP（vite dev の HMR とは `DEV_CSP=1` で分離）+ CSRF Origin 検査 + ratelimits / observability + 最小ログイン UI + Node 単体テスト 38 件、ローカル ORIGIN を 5273 に修正（#14。skill の UNVERIFIED 3 件を実物で解消、rate limiter は現行版ならローカル実動 = 書き戻しメモは PR 本文）
- 2026-08-24 Workers Builds の deploy command が本番マイグレを当てることを実証（#11 merge 直後、ホストの `wrangler d1 migrations list --remote` = 未適用 0 件）。以後マイグレを含む PR は merge するだけでよい（#12、dev-environment.md §3 に恒久記録）
- 2026-08-24 縦切り PR1 merge: 6 テーブル（user/credential/session/post/tag/post_tags）の実スキーマ + Drizzle 配線（ルート無し）、additive 検査 migrations.test.ts を CI に。0000_init との連番衝突は 0001 へずらして解決（#11、skill cloudflare-d1-drizzle-migration）
- 2026-08-23 push 起動の Workers Builds を実証（`main` の #7 / #8 / #9 の各 commit に check-run `Workers Builds: kokemusu` が success）。Phase 0 の残件が消えた
- 2026-08-23 対話シェルを `op run` で包むのをやめ（TTY が壊れる）、`shell.sh` に TTY 判定と `op://` パーサの堅牢化、`.claude/settings.json` の deny の穴（refspec 形の push / ブランチ削除）を塞ぐ（#8 / #9）
- 2026-08-23 最初の縦切り「投稿 → タグ → ヒートマップに 1 マス点く」を PR1〜6 に分割（`docs/plans/vertical-slice.md`）。決めること 4（UI トーン）・6（タグ構造）・9（集計の「日」）を確定、`RP_ID = kokemusu.shiraoka.workers.dev` を永久固定、GitHub token を `./shell.sh` の exec 時シェルへ移動（#7）
- 2026-08-23 進捗管理を status hub 化: `docs/status.md`（40 行上限）+ `docs/log.md` + SessionStart hook + `/handoff` + CI の上限検査。CLAUDE.md を規約のみに圧縮（#6）
- 2026-08-23 Workers Builds 接続完了。本番 `/health` 200・SPA 配信・`0000_init` 適用。My Profile で作った手作りトークンは picker に出ず「新しいトークンを作成する」で生成（skill `cloudflare-workers-builds-keyless-deploy` 0.3.0 = okayus-skills #24）。push 起動のビルドは未実証
- 2026-08-23 #3（docs drift）・#5（grill: ADR-0001 本文暗号化 / ADR-0002 受け側 PAT / `CONTEXT.md`）・#4（歩く骨格 `apps/web`、CI を typecheck/build/test 化、`database_id` 実 UUID）を merge
- 2026-08-22 サンドボックス再同期（node:24）・1 リポ限定 PAT を 1Password で注入（`./up.sh`）・MCP（cloudflare-docs / context7）・docs egress 10 ドメイン・`modern-web-guidance` 同梱・CI placeholder・`main` ruleset。E2E は #1、docs は #2
- 2026-06-05 デプロイ方式 = Cloudflare Workers + D1 に決定（roadmap「決めること」1）
